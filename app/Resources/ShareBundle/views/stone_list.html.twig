
<!-- catalog-section -->
<section class="simple-section">
    <div class="container">

        {{
            sonata_block_render(
                { 'type': 'app.block.service.breadcrumb' }
            )
        }}

        {{
            sonata_block_render(
                { 'type': 'share.block.stones' },
                {
                    'title': app.request.get('letter') ? 'Камни на букву «' ~ app.request.get('letter') ~ '»' : 'Все камни',
                    'show_letters': true,
                    'letter' : app.request.get('letter')
                }
            )
        }}
    </div>
</section>
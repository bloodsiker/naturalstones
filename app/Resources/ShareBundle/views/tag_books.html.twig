<div class="col-cont">
    <div class="container">

        <div class="section-title clearfix">
            {% set countBook = tag.getBooks().count() %}
            <h2>Тег <span>#{{ tag.name }} - {{ countBook }} {{ book_change_end(countBook) }}</span></h2>
        </div>

        <div class="movies clearfix grid grid-list">

            {{
            sonata_block_render(
                { 'type': 'book.block.list' },
                {
                    'items_count' : 15,
                    'page': app.request.get('page') ? app.request.get('page') : 1,
                    'tag': tag,
                }
            )
            }}

        </div>
    </div>

</div>
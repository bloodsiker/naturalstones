
<!-- catalog-section -->
<section class="simple-section">
    <div class="container">

        {{
            sonata_block_render(
                { 'type': 'app.block.service.breadcrumb' }
            )
        }}

        {{
            sonata_block_render(
                { 'type': 'share.block.stones' },
                {
                    'title': app.request.get('letter') ? 'frontend.stone_letter'|trans([], 'ShareBundle') ~ ' «' ~ app.request.get('letter') ~ '»' : 'frontend.all_stone'|trans([], 'ShareBundle'),
                    'show_letters': true,
                    'letter' : app.request.get('letter')
                }
            )
        }}
    </div>
</section>